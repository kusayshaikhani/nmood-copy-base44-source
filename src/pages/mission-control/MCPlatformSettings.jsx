import PlatformOperationsCenter from '@/components/mission-control/ops/PlatformOperationsCenter';

/** FM-011 — Platform Settings consolidated into the Platform Operations Center. */
export default function MCPlatformSettings() {
  return <PlatformOperationsCenter initialTab="settings" />;
}