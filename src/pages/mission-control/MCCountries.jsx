import { Navigate } from 'react-router-dom';

/** FM-010 — Countries consolidated into the Business Intelligence Center. */
export default function MCCountries() {
  return <Navigate to="/mission-control/analytics" replace />;
}