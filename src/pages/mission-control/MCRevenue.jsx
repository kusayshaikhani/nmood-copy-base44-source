import { Navigate } from 'react-router-dom';

/** FM-010 — Revenue consolidated into the Business Intelligence Center. */
export default function MCRevenue() {
  return <Navigate to="/mission-control/analytics" replace />;
}