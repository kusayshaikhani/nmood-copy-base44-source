import { Navigate } from 'react-router-dom';

/** FM-010 — Languages consolidated into the Business Intelligence Center. */
export default function MCLanguages() {
  return <Navigate to="/mission-control/analytics" replace />;
}