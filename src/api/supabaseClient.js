const baseUrl=import.meta.env.VITE_SUPABASE_URL?.replace(/\/$/,''); const key=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY; const sessionKey='nmood.supabase.session'; function config(){if(!baseUrl||!key)throw new Error('Nmood is not connected to Supabase yet.');} export function getSupabaseSession(){try{return JSON.parse(window.localStorage.getItem(sessionKey)||'null')}catch{return null}} export function setSupabaseSession(session){if(!session?.access_token)throw new Error('A valid session is required.');window.localStorage.setItem(sessionKey,JSON.stringify(session))} export function clearSupabaseSession(){window.localStorage.removeItem(sessionKey)} async function request(path,options={}){config();const session=getSupabaseSession();const response=await fetch(baseUrl+path,{...options,headers:{apikey:key,Authorization:'Bearer '+(session?.access_token||key),'Content-Type':'application/json',...(options.headers||{})}});const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message||body?.error_description||'Supabase request failed.');return body} export const supabaseAuth={async signInWithPassword(email,password){const session=await request('/auth/v1/token?grant_type=password',{method:'POST',body:JSON.stringify({email,password})});setSupabaseSession(session);return session},async signUp(email,password,data={}){return request('/auth/v1/signup',{method:'POST',body:JSON.stringify({email,password,data})})},async signOut(){await request('/auth/v1/logout',{method:'POST'}).catch(()=>{});clearSupabaseSession()},async getUser(){return request('/auth/v1/user')}}; export async function callSupabaseRpc(functionName,params={}){return request('/rest/v1/rpc/'+functionName,{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(params)})}
